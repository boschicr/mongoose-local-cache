{
  "name": "mongoose-local-cache",
  "version": "0.0.4",
  "description": "Plugin to cache Mongoose MongoDB query results in local LRU cache",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "BSD",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "lru-cache": "git://github.com/boschicr/node-lru-cache",
    "underscore": "^1.7.0"
  },
  "devDependencies": {
    "async": "^0.9.0",
    "mocha": "^2.0.1",
    "mongoose": "^3.8.18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/boschicr/mongoose-local-cache.git"
  },
  "keywords": [
    "mongodb",
    "mongoose",
    "cache"
  ]
}
